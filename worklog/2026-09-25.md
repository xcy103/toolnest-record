# Work Log - September 25, 2026 (Friday)

## Status

- Task date: September 25, 2026 (Friday).
- Development completed and submitted: September 26, 2026 (Saturday), at the owner's request after the September 25 Codex failure.
- Task: post-wave CSV reliability checks. The previous ten-tool roadmap ended on September 24; no new feature had been scheduled for September 25.
- Standard daily allocation: 4.5 hours. Actual owner work / learning hours are not confirmed; the allocation is not presented as measured time.
- Code commit: `17cb364`.

## Delivered

- Fixed the shared CSV parser to remove a leading UTF-8 BOM before reading quoted fields. BOM characters within cell content remain intact.
- Fixed the CSV viewer's asynchronous file-read race: selecting another file, pasting data, loading the example or clearing input invalidates earlier reads. Obsolete successes and errors cannot overwrite current input.
- Made Clear available while the first file is still being read and when a file error is shown.
- Added parser regression coverage and a deterministic event-handler test using deferred file reads.
- Kept all processing local, without paid APIs, uploads or additional infrastructure. The tool count remains 50.

## Verification

- `npm test`: 125 passed, 0 failed.
- `npm run lint`: passed.
- `npm run build`: passed, including TypeScript and generation of 131 static pages.
- Browser checks: English BOM-containing CSV displays the correct two columns and one data row; Clear removes the result; Chinese example displays four rows and four columns.
- Race regression cases: stale success after paste, stale failure after example, Clear during a pending read, and overlapping file selections.
- Automated event-handler tests use a minimal React harness, not a full browser file-upload test. Mobile viewport QA was not repeated for this non-layout change.

## Learning Topics

- Why a leading byte-order mark must be handled before CSV quote parsing.
- How generation counters prevent out-of-order asynchronous results from committing stale state.
- How deferred promises make race-condition regression tests deterministic.
- Learning topics reflect the delivered work; owner study time has not been independently measured.

## Next

- Agree on the next lightweight tool wave. Domain and license choices remain owner decisions.
- No separate Saturday workday entry is added; this entry tracks the requested Friday task and its actual completion date.
